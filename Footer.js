export default function Footer() {
  return (
    <footer className="border-t border-line px-4 py-8 text-center font-mono text-[10px] text-grayText">
      PIT — the internet picks the winner.
    </footer>
  );
}
